<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oahgnez.kotlin.ngca">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".app.NgcaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".module.novel.activity.MajorsActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".module.novel.activity.BooksByCatsActivity"/>
        <activity android:name=".module.novel.activity.BooksByRankActivity"/>

        <activity android:name=".module.novel.activity.BookInfoActivity"/>
    </application>
</manifest>
